# Core
lancedb>=0.6
sentence-transformers>=2.7
pypdf>=4.0

# File-type extractors (optional at runtime: a missing one skips that type,
# never crashes ingestion — see _extract_chunks in server.py)
python-docx>=1.1           # .docx
python-pptx>=0.6           # .pptx
beautifulsoup4>=4.12       # .html / .epub
striprtf>=0.0.26           # .rtf
EbookLib>=0.18             # .epub
openpyxl>=3.1              # .xlsx
rapidocr-onnxruntime>=1.3  # image OCR (heavy: pulls onnxruntime)
Pillow>=10.0               # image loading for OCR
