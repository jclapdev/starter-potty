{
  "vault": {
    "command": "{{PYTHON}}",
    "args": ["{{VAULT_MCP_SERVER}}"],
    "env": { "VAULT_PATH": "{{VAULT}}" }
  },
  "kb": {
    "command": "{{KB_VENV_PYTHON}}",
    "args": ["{{KB_SERVER}}"]
  }
}
